import styled from "styled-components";

const Div = styled.div`
  /* .sales > h1 {
    margin: auto 0;
    margin-right: 5%;
  }

  .sales {
    margin: 1em 0 0 0;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  } */
`;
export default Div;
